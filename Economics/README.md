# Requires notebook extensions!

1. `pip3 install --user jupyter_contrib_nbextensions`
2. `jupyter contrib nbextension install --user`
3. Launch Jupyter
4. Open Nbextensions tab
5. Enable `Python Markdown`
